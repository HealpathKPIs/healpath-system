# HealPath Executive BI â€” Project Context

**Single source of truth for future AI sessions.** Last updated: 2026-07-17 (after Sprint 45 Executive Export Center rewrite).
Read this first. Where it disagrees with memory, trust the code â€” verify claims against the repo before acting.

---

## 1. Architecture

- **Framework:** Next.js 14.2.5 (App Router), React 18, TypeScript. Server components render pages; API routes are thin wrappers.
- **Styling:** hand-written CSS design system in `app/globals.css` (semantic classes, premium executive look from Sprint 3). **No Tailwind, no PostCSS.** Do not introduce them.
- **Theme system (Sprint 28A):** light + dark, driven entirely by the CSS variables. `app/globals.css` has a `html[data-theme="dark"]` block that remaps every design token (surfaces/text/border/accent/semantic/**chart palette**/shadows) plus overrides for the few rules that hardcoded light colors. `components/ThemeManager.tsx` (mounted in `layout.tsx`) reads `hp-settings.appearance` (`light`/`dark`/`system`) and sets `<html data-theme>`; it reacts to the Settings live event `healpath:settings-changed`, cross-tab `storage`, and (in System mode) the OS `prefers-color-scheme`. An inline no-flash script in `layout.tsx <head>` applies the theme before first paint (`<html suppressHydrationWarning>`). Settings â†’ Appearance switches instantly. **When adding UI, use the CSS variables â€” do NOT hardcode hex colors, or it won't theme.** Known residual light spots (transient popovers, inline hex predating the theme): `SearchBox` autocomplete dropdown and `Donut` hover tooltip â€” small follow-up.
- **Premium motion system (Sprint 28B):** centralized in `app/globals.css` motion tokens/keyframes. Page transitions fade + slide from `translateY(12px)` over 220ms; Overview KPI cards stagger at 0/60/120/180/240ms and use the existing card components; KPI hover lifts `-2px` with stronger shadow and accent border. Drawers/palette are normalized to 200ms fade + slide with no scale. Chart containers use a first-render-only entrance (opacity 0 to 1, scale .98 to 1, 300ms); `PageTransition` stores `hp-chart-motion-seen` in `sessionStorage` so later filter/search refreshes stay quiet. Route loading uses skeletons for KPI cards, charts, and Executive Feed. All motion respects `prefers-reduced-motion`.
- **Performance Matrix (Sprint 29/29A/29B):** `/performance` page only; no nav/routing redesign, backend, schema changes, migrations, or API redesign. The page reuses existing live query functions (`getKpis`, `getDrugs`, `getDiagnostics`, `getTrends`, `listMonths`, `listSpecialties`, `listDoctors`, `resolveFilters`) plus grouped read-only matrix aggregation in `lib/queries.ts`. The matrix supports Doctors, Specialties, Medications, Laboratories, and Scans through one page-local client matrix component and the same row/cell interface. Sprint 29B fixed row aggregation: each cell is now Entity + Month via `getPerformanceEntityMetrics`, not a repeated global monthly KPI fallback. Labs/scans use `lab_fact.tests` / `scan_fact.tests` visit populations, default to Visits when their tabs open, reuse SearchBox/filters/sorting/heatmap/tooltips, and open the existing Executive Scenario panel through DashboardContext + the scenario event.
- **Chronic Module Foundation (Sprint 30):** `/chronic` is an additive page inside the same Next.js app, same layout/sidebar/theme/settings/login shell. It contains placeholders only for Executive Summary, KPI Cards, Trends, Issues, and Recommendations. No charts, database tables, APIs, import flow, backend, Supabase changes, or redesign were added.
- **Chronic Import Center (Sprint 31):** `/chronic/import` is UI-only and client-side. It reuses the existing Import Center visual pattern to accept an Excel workbook, read only `Pre` and `Post` sheets, validate sheet presence / required columns / week detection, show Pre and Post previews, and keep the import button disabled with the Sprint 32 backend message. No Supabase connection, SQL, backend API, upload, database write, or redesign was added.
- **Chronic Import Backend (Sprint 32):** `/chronic/import` now imports validated `Pre` and `Post` Excel rows into Supabase Postgres through `POST /api/chronic/import`. The route creates `healpath.chronic_pre`, `healpath.chronic_post`, and `healpath.chronic_import_batches` if missing, validates required columns and required row values, rejects duplicate Week + Patient ID + Medication Name records, inserts Pre/Post rows transactionally, and records one completed batch row. No dashboard, charts, KPIs, or redesign were added.
- **Chronic Executive Dashboard v1 (Sprint 34, supersedes the Sprint 33 overview):** `/chronic` is the full executive dashboard over the chronic tables via `getChronicOverview` in `lib/queries.ts`, with **Period as the analytical timeline** (one aggregate per business-calendar period). Filters: Period / Consultant / Recommendation / Issue / Patient Search. **One SQL fetch** (other filters bound; period applied in memory) so "delta vs previous period" works while a period is selected — current period = selected, previous = the prior period in the data; All uses the complete scoped dataset. Renders: **9 KPI cards** (Patients, Pre Patients, Post Patients, Medications, Recommendations, Issues, Avg Meds/Recs/Issues per Patient — each with `AnimatedNumber`, mini trend across periods, delta vs previous period); deterministic **Executive Summary** (issues/recommendations/medication-load/patient-volume vs previous period); exactly **4 Executive Alerts** (Highest Issue, Highest Recommendation, Highest Improvement, Largest Regression); **Biggest Movers** (single top improvement + top regression, current vs previous period, POST rows); **4 trend charts** (Patients/Recommendations/Issues/Medications across periods); **Top Issues / Top Recommendations** through `BarRank` with count + share % + period-over-period direction in the label. Counting semantics unchanged (volumes from POST rows; averages per post patient). Legacy rows whose week isn't in the calendar are excluded from period analytics. No Patient Explorer, matrix, or drill-down.
- **Chronic Query Audit (Sprint 34):** `getChronicOverview` now treats Period = All as the full scoped chronic dataset, with no hidden latest month/week default. Verified All KPI totals against imported data: Patients 6,289, Medications 33,339, Recommendations 33,339, Average Medications / Patient 5.30, Average Issues / Patient 2.58. Issue filter options are extracted from values inside `Issue 1`, `Issue 2`, ... fields and suppress issue column names such as `Issue 1` / `Issue1`; selecting an Issue filters by extracted issue values. No UI redesign, import changes, SQL/schema changes, API changes, or auth changes.
- **Chronic Data Normalization Layer (Sprint 35):** ALL normalization logic lives in **`lib/chronic-normalizer.ts`** (pure, client-safe, generic `createChronicNormalizer({canonical, mappings, threshold})` — reusable for Issues today and Recommendations/Consultants/Medication Names/Diagnoses later). During import, `/api/chronic/import` runs every parsed row through `normalizeChronicRow()` **before anything is written**: every `Issue 1..N` field inside `row_data` is normalized, the `issue` summary column is rebuilt from the normalized fields (they can never disagree), and the recommendation passes through the (rule-less, accept-as-is) recommendation normalizer. Issue pipeline: normalize key (trim/quotes/case/`issue N` prefix/`:;,-_`/punctuation/spaces) → **explicit mapping rules** (No Chronic Found & NO NEED FOR CHRONIC → Cannot be taken as chronic; Not related to diagnosis → Not related to the diagnosis; moderate & Moderate interactionue → Moderate interaction; As is, re, 1 → 0) → **fuzzy ≥ 90%** against the **13 official categories** → else keep original ("unknown"). Import report (Rows/Normalized/Mapped/Unknown) is returned by preview+import and shown on `/chronic/import` after Validate (page only *calls* the module). Audit trail: **`healpath.chronic_normalization_log`** (batch_id, field, original, normalized, reason, occurrences, imported_at), written in the same import transaction. Note: `lib/queries.ts` retains its own Sprint 34.2 read-path canonicalization for rows imported before Sprint 35 — idempotent for normalized data; unify later.
- **Chronic Analytics (Sprint 35):** `/chronic/analytics` is a second chronic page that reuses the HealPath layout, cards, filters, and chart vocabulary. It is powered by the existing `getChronicOverview` chronic row fetch plus derived in-memory analytics only: Issue Analytics (Top Issues, Distribution, Pareto 80/20, Period Trend), Recommendation Analytics (Top Recommendations, Acceptance, Trend), Consultant Analytics (Patients, Recommendations, Issues, Average Medications), and Medication Analytics (Top Medications, Recommendations, Issue Rate). Every section supports Period / Consultant / Issue / Recommendation filters. `/chronic` exposes a header Analytics action. No Patient Explorer, SQL/schema/import changes, API route, or redesign were added.
- **Chronic Intelligence Center (Sprint 36, supersedes Sprint 35 analytics):** `/chronic/analytics` is now the complete Chronic Intelligence Center combining Issue Intelligence and Recommendation Intelligence into one executive analytics experience. It reuses the existing chronic query layer through `getChronicOverview`: chronic rows are fetched once, Medication is applied in memory, and all KPI, ranking, correlation, consultant, medication, and deterministic insight aggregates are derived from that row set. Global filters: Period / Consultant / Recommendation / Issue / Medication / Patient Search. Renders: 7-KPI top strip (Total Issues, Total Recommendations, Recommendation Acceptance %, Issue Density, Recommendations per Patient, Issues per Patient, Improvement %), Issue Intelligence (Top 10, Trend, Distribution, Severity, by Consultant/Medication/Period), Recommendation Intelligence (Top 10, Trend, Distribution, by Consultant/Medication/Period), Issue-to-Recommendation correlation pairs, sortable Consultant Intelligence table, Medication Intelligence, and deterministic Executive Insights. Added `/chronic/analytics/loading.tsx`. No Patient Explorer, matrix, import, SQL/schema/database, API route, or auth changes.
- **Chronic Dashboard Refactor (Sprint 36R, Power BI business logic):** `/chronic` was refactored away from generic analytics/rankings into the Power BI business story: a PRE vs POST clinical outcome dashboard. It keeps HealPath UI/components but centers on phase comparison. Renders: Executive Comparison cards for Patients, Medications, Issues, Recommendations, Avg Medications/Patient, Avg Issues/Patient, Avg Recommendations/Patient; each card shows PRE, POST, Difference (POST - PRE), and Improvement % (`(PRE - POST) / PRE`). Clinical Outcome shows two PRE/POST line charts: Issues per Patient and Recommendations per Patient, with improvement %. Issue Comparison and Recommendation Comparison now render complete fixed catalogs, never Top N and never popularity-sorted. Recommendation catalog follows the Power BI order: As Is, To be re-evaluated, Monitored, To be stopped, Adjusted, Modified. Issue catalog follows the PDF order using catalog hints mapped to full live labels, then appends any unmatched configured labels. Operational KPIs: Waiting Lab, No Need For Chronic, No Need %. Filters: Period / Consultant / Patient Search / Issue / Recommendation. No Patient Explorer, matrix, import, parser, SQL/schema/database, API route, auth, Pareto, treemap, correlation, movers, or executive feed changes. Build note: the single requested build compiled and type-checked, then failed during page-data collection on unrelated `/api/admin/import` module resolution; this was left untouched under the Sprint 36R scope fence.
- **Chronic Performance Optimization (Sprint 36):** `/chronic` now uses `getChronicPageData`, an aggregate-only query path for the PRE vs POST dashboard. `getChronicOverview` remains available for `/chronic/analytics`, but `/chronic` no longer fetches every chronic row or ships `row_data` to Node. The page loads KPI aggregates, chart trend data, filter options, issue counts, recommendation counts, operational counts, and calendar options with parallel SQL via `Promise.all`; a short-lived per-request promise cache prevents duplicated SQL across Suspense sections. Independent Suspense boundaries wrap filters, Executive Comparison, Clinical Outcome, Issue Comparison, Recommendation Comparison, and Operational KPIs, with KPI cards before charts. Added missing chronic indexes on both Pre/Post tables for patient_id, period, week, consultant expression, recommendation, and issue. Verified old vs optimized output matched for metrics, trends, catalogs, operational KPIs, and options; optimized data helper improved from 111,014 ms to 9,439 ms. No UI redesign, API change, import change, or business-logic change.
- **Executive KPI Redesign (Sprint 37):** `/chronic` KPI cards were presentation-polished only. PRE/POST cards now display compact executive values (`41.2K`, `33.3K`, `1.25M`), a signed compact Difference value, and a green/red/gray improvement badge. Typography, spacing, hierarchy, and responsive fit were tightened while reusing the existing HealPath card shell, hover lift, design tokens, and Suspense layout. Operational KPI cards received the same executive spacing and compact numeric treatment. No metric logic, query, API, import, or business-rule changes.
- **Executive KPI Cards Polish (Sprint 37.1):** `/chronic` executive KPI cards received a production-quality presentation-only redesign. Cards are taller with 18px radius, larger padding, more whitespace, softer shadow, stronger hover elevation/border, equal-width PRE/POST columns that cannot touch, large bold compact executive numerals (`6.3K`, `41.2K`, `33.3K`, `7.9K`), clear top/title + top-right improvement badge + middle PRE/POST + bottom Difference/Trend hierarchy, muted smaller difference values, and responsive spacing. Operational KPI cards use the same executive shell. No calculations, query logic, business logic, API, import, database, or page-layout redesign changes.
- **Executive Charts (Sprint 38):** chronic chart surfaces now support an executive drill modal via `app/chronic/ExecutiveChartDrill.tsx`. `/chronic` Clinical Outcome charts and `/chronic/analytics` trend, BarRank, and distribution charts open an 80vw modal with large line/bar rendering, zoom, pan, reset zoom, richer hover tooltip, legend, PNG export, CSV export, Close, Esc close, and click-outside close. Chart calculations and query outputs are unchanged; existing chart data is passed into the drill wrapper as-is. No API, import, database, or business-logic changes.
- **Executive Interactive Analytics (Sprint 38.1):** the chronic chart drill modal is now an executive interactive analytics surface. It keeps the same chart datasets and calculations, but adds crosshair hover, selectable periods/bars, richer hover values, smooth chart transform motion, an analytics drilldown panel, and CSV export that can include the selected drill context. `/chronic` Clinical Outcome charts pass the existing filtered `getChronicPageData().drilldowns` payload into the chart wrapper, so Top Issues, Top Recommendations, Top Consultants, Top Medications, Top Patients, Weekly Breakdown, and Month Comparison use already-aggregated dashboard data. No query, calculation, API, import, database, or business-logic changes.
- **Chronic Executive Card and Drill Modal visual fix:** `/chronic` executive KPI cards now use a chronic-specific wider grid so PRE/POST values no longer collide or clip at desktop widths. Executive card numerals are container-aware, POST values align to the right side of their column, and the chronic dashboard has a wider centered canvas. The Clinical Outcome drill modal now opens as a wider chart-plus-drill workspace, defaults to the latest period, keeps chart and drilldown panels side-by-side on desktop, and stacks them responsively on mobile. No calculations, queries, API, import, database, or business logic changed.
- **Chronic chart side-drill value fix:** the Clinical Outcome chart modal no longer depends on a floating tooltip for PRE/POST values. Hovering any month now updates the side drill panel itself with chart-specific values for the active chart (`Issues per Patient` or `Recommendations per Patient`): PRE, POST, Difference, and Improvement %. The floating line-chart tooltip was removed to avoid clipping/disappearing against modal and viewport dimensions. No calculations, queries, API, import, database, or business logic changed.
- **Advanced Chronic Analytics (Sprint 39):** `/chronic/analytics` now includes an Executive Ranking Analytics block with Top Issues, Top Recommendations, Top Consultants, and Top Medications. Each section shows Top 10 and Bottom 10 horizontal ranking bars, supports Value/Label sort links that preserve active filters, and reuses existing filtered chronic analytics data plus the Sprint 38 drill wrapper. Consultant and medication analytics pools were widened for complete Bottom 10 ranking coverage, while existing Medication Intelligence display remains capped. No UI redesign, API route, import change, SQL/schema change, or chart-calculation change.
- **Patient Explorer (Sprint 40):** `/chronic/patient` is a read-only patient-level chronic explorer. It supports Patient ID search, partial-match patient selection, summary cards, a PRE to POST week timeline, Medication Changes, Issue History, Recommendation History, and Week History. Data comes from `getChronicPatientExplorer` in `lib/queries.ts`, which resolves patient matches first and then fetches only the selected patient's PRE/POST rows; issue values are extracted from numbered Issue fields and canonicalized through the existing read path. `/chronic` exposes a header Patient action. No editing, API route, import change, SQL/schema/database change, auth change, or dashboard KPI logic change.
- **Executive Drilldown (Sprint 41):** every `/chronic` KPI card is now clickable and opens a modal drilldown. Executive comparison KPIs reuse `getChronicPageData` drill aggregates for Top Weeks, Top Consultants, Top Patients, Top Medications, and KPI-specific category/trend views; operational KPIs expose the same modal navigation where data exists. The modal is client-side, keyboard accessible (Enter/Space to open, Esc/click-outside to close), and reuses HealPath card, bar ranking, token, and modal language. No API route, import, SQL/schema/database, authentication, chart-calculation, or dashboard business-logic changes.
- **Export Center (Sprint 42):** `/chronic` now includes a client-side Export Center fed by the already-filtered `getChronicPageData` payload. It exports Dashboard KPIs, Clinical Outcome chart datasets, KPI drilldown rankings, and fixed catalog tables as Excel (`.xls` HTML workbook), CSV, browser print-to-PDF, and PNG summary image. Exports respect current Period / Consultant / Issue / Recommendation / Patient filters because they use the same filtered server data rendered on the dashboard. No new API route, query duplication, import change, database/schema change, authentication change, or dashboard business-logic change.
- **Final Polish (Sprint 43):** `/chronic` received production-readiness polish without changing dashboard business logic. Loading now mirrors the real page structure (header actions, filters, export center, KPI strip, charts, fixed tables, operational KPIs); a route-level `app/chronic/error.tsx` provides retryable error recovery; catalog tables now render explicit empty states; chronic-specific responsive/focus CSS improves mobile chart/table behavior and modal sizing; keyboard shortcuts were added (`Alt+F` patient search, `Alt+E` export center, `Alt+1..5` dashboard sections) through an accessible hidden helper; KPI and chart drill modals gained arrow/zoom/reset keyboard behavior and clearer step/button state; Export Center derived rows are memoized and export actions announce status to assistive tech. Local dead/stale code cleanup removed the old mismatched `/chronic` loading skeleton shape. Build passed. No API, import, SQL/schema/database, auth, or query-business-logic changes.
- **Patient 360 (Sprint 38 Patient 360):** added `/patient-360`, a read-only combined acute + chronic patient page using the existing HealPath shell, cards, tabs, tables, empty states, and CSS tokens. Sidebar navigation now places Patient 360 between Chronic Care and Performance Matrix. Search is exact Patient ID only (`?patient=`), with no autocomplete. `getPatient360` in `lib/queries.ts` is the one query-layer entry point and uses multiple small parameterized queries to load acute visits plus child diagnosis/drug/lab/scan rows by `visit_id`, chronic PRE/POST reviews by `patient_id`, medication history, a unified acute/chronic timeline, and the fixed chronic issue catalog. No import, parser, authentication, existing dashboard, API route, or schema changes.
- **Chronic Import Navigation (Sprint 33A):** `/chronic` now exposes a header Import action that links directly to `/chronic/import`. The Chronic Import Center keeps the existing Upload -> Preview -> Import workflow and adds browser-local Import History using the same table/card pattern as the Admin Import Center. No database changes, API changes, dashboard changes, upload component rewrite, or redesign were added.
- **Chronic Business Calendar (Sprint 33.5 → Business Calendar Persistence):** Chronic Week is an **internal integer field**; **Period** ("Jun 2026") is the primary, user-facing timeline. The mapping is **data, not code**: the single source of truth is the DB table **`healpath.chronic_calendar`** (`week` PK, `month_name`, `year`, `month_order`, `period`), created + seeded with weeks 1–28 (Dec 2025→Jun 2026, 4/month) idempotently by `/api/chronic/import` (`create table if not exists` + seed `on conflict (week) do nothing`, so it never overwrites manually-added weeks). **Adding weeks 29, 30, 31 … requires only inserting calendar rows — no code change** (verified: a week-29 insert made both import and dashboard handle "Jul 2026" with zero code edits). `lib/chronic-calendar.ts` is now a **pure, calendar-agnostic** helper (its lookups take a calendar array) plus `CHRONIC_CALENDAR_SEED` (the bootstrap 1–28 + offline fallback) — shared by the query layer, the import route, and the client preview.
  - **Import** resolves every row's Week **through the DB calendar** and stores `month_name`, `year`, `period`, `month_order` (+ canonical `month` YYYY-MM) per row (additive columns). Multi-week uploads allowed; response gives Periods Found / Weeks Found (range) / Missing Weeks (informational — a partial period is valid, never an error). A week absent from the calendar is a clear error ("add it to healpath.chronic_calendar"). `GET /api/chronic/import` returns the calendar so the **client preview** ("Detected Periods") is DB-driven too (seed fallback).
  - **Dashboard** (`/chronic`) filter is **Period** (options are periods present in the data, ordered chronologically; Week/Month filters removed). `getChronicOverview` loads the calendar via `getChronicCalendar()` (DB, seed fallback) and maps the selected period → its week list, filtering on that; trend labels use the period. Stale pre-existing ISO-week rows (`2026-W32`) don't resolve to any calendar week and are gracefully excluded from Period options.
- **Chronic Template Validation Fix (Sprint 33B):** `/chronic/import` validation now follows the uploaded chronic workbook headers instead of generic column names. Patient ID accepts template aliases including `INDIVIDUAL NUMBER`, `Individual NBR`, `Patient ID`, and `ID`; Week accepts `Week`; Recommendation accepts singular/plural forms; Issues are detected as one or more normalized headers starting with `Issue` (`Issue 1`, `Issue1`, `ISSUE_1`, `Issue-1`, etc.). Comment columns are optional and ignored by validation. The preview shows detected columns for Patient ID, Week, Recommendation, and the number of Issue columns. No template edits, database changes, API changes, or redesign were added.
- **Shared Chronic Parser (Sprint 33.5C):** Chronic workbook parsing now has exactly one implementation in `lib/chronic-parser.ts`. Both the `/chronic/import` client preview and `/api/chronic/import` server import call the shared parser for header normalization, Patient ID / Week / Recommendation aliases, Issue-column detection, workbook validation, and parsed row creation. This removes the former client/server header mismatch. No database changes, API redesign, or UI redesign were added.
- **Charts:** `recharts` for the donut; custom inline SVG for `TrendLine` and `BarRank`.
- **Sprint 27 UX notes:** the Sprint-24 Executive Scenario full-height drawer is now a **compact floating panel** (bottom-right, `ExecutiveScenarioLayer` in `components/ExecutiveExperience.tsx`) â€” shows only while an entity is selected; its **X clears the selection** (context + `?sel/?selv`, and `?doctor` when doctor-driven). Browser-local preferences (no server persistence): `hp-settings` (`{appearance, animations, showFeed}` â€” written by `/settings`, `showFeed` read by `ExecutiveFeed`) and `hp-upload-history` (latest 10 imports, written by `/admin/import`, shown as the Upload History table). `/settings` shows Version (package.json) and Database Status via the existing `/api/kpis`.
- **Compare Center (Sprint 25, UI-only):** `components/CompareCenter.tsx` â€” a right-side drawer opened by the **âڑ– Compare** button rendered inside `FilterBar` (so it sits beside the global filters on every page). Supports exactly Doctor-vs-Doctor, Medication-vs-Medication, Month-vs-Month. Each side's profile is fetched from the **existing API routes only** (`/api/kpis`, `/api/drugs`, `/api/diagnostics`, `/api/trends`, `/api/specialties`); unavailable metrics are hidden, never fabricated â€” medication compare therefore shows **Prescriptions** only (the existing APIs accept no medication filter). Quick Actions navigate via the existing filters (`?doctor=`, `?month=`, `?sel=drug&selv=`).
- **Command palette (Sprint 23, UI-only):** `components/CommandPalette.tsx` (mounted in `layout.tsx`, opened by Ctrl/Cmd+K or the Nav search button via a `healpath:command-open` window event). It is a **launcher, not a search engine** â€” it fans out to the existing `/api/search` for all four scopes (doctors/pharmacy/diagnostics/diseases), round-robin merges to 8 results with icon + title + category badge, and on select navigates to the entity's page with the existing `?q=<value>` filter (`/doctors|/pharmacy|/diagnostics|/diseases`). Keyboard: â†‘/â†“/Enter/Esc; closes on Esc / outside-click / select.
- **UI primitives (Sprint 21, UI-only):** `components/PageTransition.tsx` wraps the page content in `layout.tsx` (fade + 8px rise, 210ms ease-out, keyed on pathname so it plays on page change only â€” not filter/search updates; respects `prefers-reduced-motion`). `components/AnimatedNumber.tsx` count-up animates KPI values (parses the already-formatted string, so call sites are unchanged; timed-tick driven so it always settles even when rAF is throttled; animates only on value change) â€” used by `KpiCard` and the Overview `OverviewKpi`.
- **Data layer (`lib/queries.ts`)** â€” every metric function returns a fixed shape and has two sources:
  - **Live:** `lib/pg.ts` â†’ direct **parameterised, read-only** Postgres over the Supabase **Session Pooler**, verified TLS (`certs/prod-ca-2021.crt`, `rejectUnauthorized` on). Bind params only â€” no string interpolation of user input. Shared `VISIT_FILTER` binds `$1` month, `$2` specialty, `$3` doctor, `$4` drug (visits containing an active ingredient/brand), `$5` disease (visits containing an ICD block); any per-query LIMIT is `$6`. `getTrends` binds `$1` specialty, `$2` doctor, `$3` drug, `$4` disease. `resolveFilters(searchParams, honor)` computes effective filters (selection > URL > default).
  - **Fallback:** `data/snapshot2026.json` (bundled). Used automatically when `hasDb` is false or a live query throws.
- **API routes** (`app/api/*`): call the same `lib/queries` functions â†’ **API contracts are stable regardless of live/snapshot source.**
- **`lib/supabase.ts`** (PostgREST client) and the exported `SQL` object in `lib/queries.ts` are now **dead code** (the `exec_sql` path was abandoned â€” see Decisions). Left in place; do not depend on them.
- **Auth (real gate since 2026-07-19):** root `middleware.ts` protects every page and API route (except `/login`, `/api/login`, static assets). It requires the **signed** `hp_auth` cookie — a SHA-256 token derived from `DASHBOARD_PASSWORD` (`lib/auth.ts`, Edge-safe Web Crypto) — redirecting unauthenticated pages to `/login?next=…` and returning 401 for APIs. `POST /api/login` verifies the password (digest comparison) and sets the token as an **httpOnly** cookie (SameSite=Lax, Secure in prod, 30d). Forged `hp_auth=1` cookies no longer pass. **The gate activates only when `DASHBOARD_PASSWORD` is set** (e.g. in Vercel env vars); without it the app is open (local-dev convenience). Not multi-user auth — one shared password.
- **Import tooling (Sprint 26 layout):** the importer's parse/clean/load/validate logic lives in **`lib/import-core.mjs`** â€” the **single source of truth**. `scripts/pg-import.mjs` is a thin CLI wrapper over it; the **Admin â†’ Data Import** UI (`/admin/import`, in the sidebar nav) uses the same core via the additive `POST /api/admin/import` route (`mode=preview` parses only â€” no DB write; `mode=import` runs the load and streams NDJSON progress). `scripts/import.ts` is the original superseded supabase-js loader. âڑ ï¸ڈ Import semantics unchanged: visits are PK-deduped (`on conflict do nothing`) but **fact rows are appended** â€” re-importing an already-loaded extract duplicates facts (the UI warns about this).

### Key files
| Path | Role |
|---|---|
| `lib/queries.ts` | All metric functions (live + snapshot fallback) |
| `lib/pg.ts` | Pooled direct Postgres, verified TLS |
| `app/globals.css` | Entire design system implementation (frozen since Sprint 3) |
| `HEALPATH_DESIGN_SYSTEM.md` | Design system **documentation** (Sprint 27B) â€” tokens, component/dashboard/interaction rules; keep in sync with `globals.css` |
| `lib/import-core.mjs` | Importer single source of truth (parse/clean/load/validate) |
| `lib/chronic-parser.ts` | Chronic workbook parser single source of truth shared by preview and import |
| `components/export/*` + `lib/export/*` | **Executive Export Center** (Sprint 45 rewrite, acute-only): `ExportToolbar` (mounted in `app/layout.tsx`, shows on the 7 acute dashboards; inert on Chronic/Patient 360/others) + PDF/PNG/Full-Report utilities. Client-side DOM capture (html2canvas + jsPDF); presentation-only. Captures `.page-enter` only after `waitForStableDashboard` confirms document load, fonts, images, no skeletons, rendered charts/tables, finished animations, and a quiet DOM/layout window. Full Report uses the same hidden-iframe capture path for every dashboard and fails fast instead of skipping/blanking pages. Toolbar carries `data-export-exclude`; the acute set lives in `EXPORT_DASHBOARDS` (`lib/export/constants.ts`). |
| `scripts/pg-import.mjs` | CLI wrapper over `import-core` |
| `certs/prod-ca-2021.crt` | Supabase CA (needed at runtime for TLS) |
| `.env.local` | `DATABASE_URL` (pooler), Supabase URL + keys â€” secrets |
| `data/snapshot2026.json` | Fallback dataset (mirrors the Power BI model) |

---

## 2. Function â†’ source status (verified in code)

| Function | Source | Used by |
|---|---|---|
| `getKpis` | **LIVE** | Overview, Pharmacy, Doctors, Diagnostics |
| `getDiseases` | **LIVE** | Overview, Diseases |
| `getDiseaseDescriptions` | **LIVE** | Diseases |
| `getDrugs` | **LIVE** | Overview, Pharmacy |
| `getTrends` | **LIVE** | Overview, Pharmacy, Doctors, Diagnostics, Trends |
| `getDiagnostics` | **LIVE** | Labs & Scans |
| `getSpecialties` | **LIVE** | Doctors |
| `listMonths` / `listSpecialties` / `listDoctors` | **LIVE** (async-warmed cache) | Filter dropdowns â€” sync `(): string[]` contract; async-warmed module cache with snapshot fallback (Sprint 14; `listDoctors` added Sprint 15, fallback = snapshot top-20 doctors) |

**Global filters (Sprint 15):** Month, Specialty, **Doctor** â€” all via URL query params (`?month=&specialty=&doctor=`), shareable, and combinable. Every metric function accepts a `doctor` filter (`Filters.doctor` / `getTrends(specialty, doctor)`). `getTrends(specialty)` callers unaffected (doctor is an optional 2nd arg). As of Sprint 18B, Doctor chart clicks use the canonical `?doctor=` param, Nav preserves URL state across pages, and DashboardContext/URL/Doctor dropdown stay synchronized.

**Cross-filter selection (Sprint 16 infra + Sprint 17 activated):** `lib/dashboard-context.tsx` provides a global `DashboardProvider` (wraps the app in `app/layout.tsx`) holding a single `selection: { type: 'drug'|'disease'|'doctor'|'specialty', value } | null` (`select()` toggle / `clear()`), read via `useDashboard()`. `BarRank`/`Donut` are client components; with a `kind` prop their rows/slices are clickable (`role=button`, `aria-pressed`, `data-selected`, cursor). A click updates the context (pressed visual) **and reflects the single selection into the URL as `?sel=<type>&selv=<value>`** so the server pages re-fetch. Server pages call `resolveFilters(searchParams, honor)` which applies **priority: selection (`?sel`) > URL dropdown (`?month/specialty/doctor`) > default**. Doctor/specialty selections reuse the doctor/specialty filter (behave exactly like the dropdown). Drug/disease are visit-population filters. Re-click clears `?sel` (back to parity). Labs/scans bars stay non-clickable.

**Sprint 18A exception/upgrade:** Doctor chart clicks now behave exactly like the Doctor dropdown: they set `?doctor=<name>` and re-click clears `?doctor`; stale `?sel/?selv` is cleared. `FilterBar` mirrors `?doctor=` into `DashboardContext`, so the chart pressed state, URL, and dropdown are synchronized.

**Trend tooltip (Sprint 18A):** `TrendPoint` includes `visits` in live data, and `TrendLine` tooltips show Month, Visits, Avg Meds / Visit, Avg Labs / Visit, Avg Scans / Visit. The existing `TrendResponse.delta` is passed to `TrendLine` and displayed on the latest point only; no new delta calculation was added.

**Sprint 18B regression fixes:** `Nav` preserves `month`, `specialty`, `doctor`, `sel`, and `selv` on every page link. Doctor row active state is URL-driven (`?doctor=`) so stale DashboardContext cannot clear a doctor when the URL is empty. Doctor row clicks use the dropdown-equivalent URL path and trigger live server data on pages that honor doctor filtering. `TrendLine` now attaches the existing tooltip text to a larger transparent SVG hover target so the native tooltip is reachable without changing chart visuals.

**Universal search (Sprint 19):** one reusable `components/SearchBox.tsx` (client) on Diseases / Pharmacy / Diagnostics / Doctors. Debounced 300ms, min 2 chars â†’ `GET /api/search?scope&q` â†’ `searchOptions(scope, q)` (SQL **ILIKE** live / snapshot **includes** fallback). Dropdown shows up to 8 hits with **highlighted** match + **keyboard nav** (â†‘â†“/Enter/Esc). Selecting a hit sets `?q=<value>` (preserving other params); pages read it via `resolveFilters` â†’ `Filters.search` and the search-enabled queries filter with `ILIKE $7`. Search scopes: Diseases = `icd_desc` + `diseases` (ICD code); Pharmacy = `ac`/`brand`/`medications`; Diagnostics = `lab_fact.tests`/`scan_fact.tests`; Doctors = `practitioner_name`/`doctor_specialty`. The old `DataTable` client-side search was removed on Diseases/Doctors (SearchBox replaces it). `?q` is **not** preserved across `Nav` (page-local search).

**Executive insights (Sprint 20):** Overview now renders a deterministic executive insights section above the KPI grid. It contains an alert bar generated from already loaded Overview data using only doctors, medications, labs, visits, Avg Medications / Visit, and Avg Labs / Visit. The former "doctors contributed" alert was removed and replaced with a Vitamin D lab insight that reuses existing `getDiagnostics` live data, showing current requests and Delta % when latest/previous month data is available. Biggest Movers ignores diseases and compares only Avg Medications / Visit, Avg Labs / Visit, and Doctors for the latest trend month vs the previous trend month, reusing `getTrends` data plus existing month-scoped `getKpis` calls. Smart Comparison appears only when `doctor` is selected; its calculation is unchanged (`getKpis(f)` vs `getKpis({ ...f, doctor: null })`) and its presentation was polished with premium cards/status chips. No new SQL, routes, API routes, database tables, filters, auth changes, or AI generation were added.

**Executive experience (Sprint 24, UI-only):** Overview now has one client presentation layer (`components/ExecutiveExperience.tsx`) that reuses existing `DashboardContext` and already-loaded Overview data. It adds a right-side Executive Scenario drawer for clicked Medication/Laboratory feed entities and existing chart selections, deterministic `Explain` popovers for the major Overview charts, and a compact Executive Feed after the charts. No SQL, API, backend, routing, filter, database, or query changes were added.

**Page honor matrix (which cross-filter each page applies):**
| Page | month | specialty | doctor | drug | disease |
|---|:-:|:-:|:-:|:-:|:-:|
| Overview | âœ“ | âœ“ | âœ“ | âœ“ | âœ“ |
| Diseases | âœ“ | âœ“ | âœ“ | âœ“ | âœ— (is the disease view) |
| Pharmacy | âœ“ | âœ“ | âœ“ | âœ“ | âœ“ |
| Labs & Scans | âœ“ | âœ“ | âœ“ | âœ— | âœ“ |
| Trends | â€” | âœ“ | âœ“ | âœ“ | âœ“ |
| Doctor & Specialty | âœ“ | âœ“ | âœ— (inert, Sprint 15) | âœ— | âœ— |

---

## 3. Page status

| Page | Route | Status | Notes |
|---|---|---|---|
| Overview | `/` | âœ… **Live + verified** (Sprint 7) | All data live |
| Disease & Diagnosis | `/diseases` | âœ… **Live + verified** (Sprint 8) | All data live |
| Pharmacy | `/pharmacy` | âœ… **Live + verified** (Sprint 10) | All data live |
| Trends | `/trends` | âœ… **Live + verified** (Sprint 12) | All data live |
| Doctor & Specialty | `/doctors` | âœ… **Live + verified** (Sprint 11) | All data live |
| Labs & Scans | `/diagnostics` | âœ… **Live + verified** (Sprint 9) | All data live |
| Performance Matrix | `/performance` | âœ… **Live + verified** (Sprint 29A) | Reuses existing live queries; Doctors/Specialties/Medications/Laboratories/Scans |
| Chronic Care | `/chronic` | Premium executive overview (Sprint 33A) | Chronic filters, KPIs, deterministic summary, alerts, movers, trends, top issues/recommendations; header Import link to `/chronic/import` |
| Chronic Import Center | `/chronic/import` | Supabase import backend + history + template-aware validation (Sprint 33B) | Pre/Post preview + import into chronic tables; browser-local import history; detected template columns; no dashboard/charts/KPIs |

Filter enumerations (`listMonths`/`listSpecialties`) are **live** as of Sprint 14 (async-warmed cache; snapshot fallback until warm/if DB down). **Every data provider is now live.**

---

## 4. Database status

- **Supabase Postgres**, schema **`healpath`**, exposed to the API (`public, graphql_public, healpath`); `service_role` has `usage` + table/sequence grants.
- **Tables & constraints:**
  - `visits` â€” PK `visit_id` (text). Cols: visit_id, patient_id, prescription_date (timestamptz), doctor_specialty, practitioner_name, month_no, month_year.
  - `diagnosis_fact`, `drug_fact`, `lab_fact`, `scan_fact` â€” PK `id` (bigint identity, auto), **FK `visit_id â†’ visits.visit_id` (enforced)**.
- **Connection:** Session Pooler `aws-0-eu-west-1.pooler.supabase.com:5432`, user `postgres.<ref>`, TLS **verified via CA cert** (URL `sslmode` is stripped in code; `ssl:{ca}` is authoritative). The direct `db.<ref>.supabase.co` host is IPv6-only and does **not** resolve here â€” always use the pooler.
- **No `exec_sql` RPC** exists (intentionally â€” creating one was blocked as an RCE surface).

### Current row counts (loaded)
| Table | Rows | Workbook | Skipped |
|---|---:|---:|---:|
| visits | 86,329 | 86,329 | 0 |
| diagnosis_fact | 116,802 | 116,808 | 6 |
| drug_fact | 207,133 | 207,136 | 3 |
| lab_fact | 68,345 | 68,355 | 10 |
| scan_fact | 10,011 | 10,014 | 3 |

Chronic tables (created by `/api/chronic/import` if missing): `chronic_pre`, `chronic_post`, `chronic_import_batches`, and **`chronic_calendar`** (the business-calendar source of truth: `week` PK, `month_name`, `year`, `month_order`, `period`; seeded weeks 1–28, extend by inserting rows). Pre/Post rows store batch_id, week, month, **month_name, year, period, month_order**, patient_id, recommendation, issue, medication_name, row_data, imported_at, with a unique Week + Patient ID + Medication Name index for duplicate protection. Batch rows store batch_id, week (range), month (period label), file_name, imported_at, pre_rows, post_rows, status, and duration_ms.

FK integrity clean (0 orphans post-load); 0 NULL `visit_id`.

---

## 5. Import status

- **Complete** via `scripts/pg-import.mjs` (Sprint 6). Source: `C:\Users\User\Downloads\HealPath_BI_Starter.xlsx`.
- 488,620 of 488,642 rows loaded in one transaction (~170s).
- **22 fact rows skipped** (6 distinct VisitIDs) â€” proven source referential gaps: their parent visits are absent from the `Visit` sheet (the 4 patient numbers have zero visits). Skipped by explicit user decision; **nothing fabricated**.
- To reach 100%: a corrected extract whose `Visit` sheet includes those 6 VisitIDs, then re-run the loader.

**Chronic import (Sprint 32):** `/chronic/import` supports the separate chronic `Pre`/`Post` workbook template and writes only to `healpath.chronic_pre`, `healpath.chronic_post`, and `healpath.chronic_import_batches`. Duplicate Week + Patient ID + Medication Name rows are rejected before any write.

---

## 6. Business rules (must be preserved)

1. **2026 reporting window** â€” the Power BI model (and every live metric) is scoped to `month_year like '2026-%'`. The DB also holds 2025 rows which the model **excludes**. Never drop this scope. (The window's visit count was **77,306** at the original import; it is data-dependent â€” **77,738** observed on 2026-07-09 after additional data was imported.)
2. **DAX â†’ SQL measures:** Visits = `count(distinct visit_id)`; Patients = `count(distinct patient_id)`; Avg Meds/Visit = `count(drug brand, non-null) / distinct visits`; Avg Labs/Scans analogous with `lab_fact.tests` / `scan_fact.tests`.
3. **VisitID cleaning:** trim + collapse internal whitespace.
4. **month_year** derived from `Year` + `MonthName`; corrupt `Year < 2000` (1970 block) folded to 2026; `prescription_date` nulled for those rows.
5. **Blank text â†’ NULL.**
6. **Filter nuances that reproduce the model exactly:**
   - Active ingredients (`drug_fact.ac`): exclude `NULL`, `''`, and `'0'`.
   - ICD **descriptions** (`diagnosis_fact.icd_desc`): exclude only `NULL`/blank â€” **keep the literal `'0'`** (the model has "0" = 3,336).
   - Brands: `lower(btrim(brand))`, exclude NULL/blank.
   - Specialty URL filters are trimmed before binding to live SQL because the model snapshot includes `Chest and Respiratory\n` while the DB stores `Chest and Respiratory`.
7. **Top-N limits:** ingredients 15, brands 10, ICD descriptions 15, ICD blocks per call (Overview 5, Diseases 10), labs 10, scans 10, doctor matrix 20.
8. **"Heal Path Polyclinic (Chronic)/(Telehealth)"** as a `Practitioner Name` is a **legitimate business identifier** (clinic booked as practitioner), not corrupt data.
9. **Doctor filter scope (Sprint 15):** the global Doctor filter (URL `?doctor=`, matched on `v.practitioner_name`, trimmed before binding) affects **Overview, Diseases, Pharmacy, Diagnostics, Trends**. The Doctors page renders the dropdown (global) but intentionally does **not** apply it (it doesn't parse `doctor`), so its ranking/matrix are unchanged.

---

## 7. Current decisions (standing)

- **Live data = direct parameterised `pg`**, NOT an `exec_sql`/arbitrary-SQL RPC (security: RCE surface on shared DB â€” explicitly avoided/blocked). Do not create arbitrary-SQL functions.
- **No shared-infra/DB-object changes** for live conversion â€” queries live in the app.
- **One page at a time**; snapshot fallback retained everywhere; conversions are **data-layer only**.
- **UI/CSS are frozen** at the Sprint 3 design. Sprints 7+ change only `lib/queries.ts` / `lib/pg.ts` / config.
- **API contracts are fixed** â€” never change return shapes.
- `listMonths`/`listSpecialties` stay **synchronous snapshot** (changing to async breaks callers).
- Orphan rows: **skip + document**, never fabricate parent visits.

---

## 8. Completed sprints

| Sprint | Outcome | Report |
|---|---|---|
| (pre) Env fix | Resolved stale `.next` causing "unstyled" render | `docs/ENVIRONMENT_VERIFICATION.md` |
| 3 | Premium executive design system (globals.css) + Nav/TrendArrow/BarRank/TrendLine polish | `docs/SPRINT3_REPORT.md` |
| 4 & 5 | Doctor + Pharmacy executive dashboards (presentation/composition, reused components) | `docs/SPRINT4_5_REPORT.md` |
| 6 | Imported production data into Supabase (`pg` loader) | `docs/DATA_IMPORT_REPORT.md` |
| 7 | Overview â†’ live (`getKpis/getDiseases/getDrugs/getTrends`), created `lib/pg.ts` | `docs/SPRINT7_REPORT.md` |
| 8 | Disease & Diagnosis â†’ live (`getDiseaseDescriptions`) | `docs/SPRINT8_REPORT.md` |
| 9 | Labs & Scans â†’ live (`getDiagnostics`) | `docs/SPRINT9_REPORT.md` |
| 10 | Pharmacy live verification (`getKpis/getDrugs/getTrends`) | `docs/SPRINT10_REPORT.md` |
| 11 | Doctor & Specialty â†’ live (`getSpecialties`) | `docs/SPRINT11_REPORT.md` |
| 12 | Trends live verification (`getTrends`) | `docs/SPRINT12_REPORT.md` |
| 13 | Power BI parity audit; fixed high-severity specialty filter newline mismatch | `docs/POWERBI_PARITY_REPORT.md` |
| 14 | `listMonths`/`listSpecialties` â†’ live (async-warmed cache, sync contract preserved) â€” **last snapshot providers converted** | `docs/SPRINT14_REPORT.md` |
| 15 | Global **Doctor filter** (URL param) across Overview/Diseases/Pharmacy/Diagnostics/Trends; added `listDoctors`; all SQL still parameterised | `docs/SPRINT15_REPORT.md` |
| 16 | Cross-filter **interaction infrastructure** â€” global `DashboardContext`; chart clicks (drug/disease/doctor/specialty) emit a shared selection (no analytics consumption yet) | `docs/SPRINT16_REPORT.md` |
| 17 | **Activated cross-filtering** â€” selection reflected to `?sel/?selv`, consumed server-side via `resolveFilters` (priority selection > URL > default); drug/disease visit-population filters; honor matrix per page; re-click clears | `docs/SPRINT17_REPORT.md` |
| 18A | Rich trend point tooltip + Doctor chart/dropdown/URL synchronization (`?doctor=` canonical) | `docs/SPRINT18A_REPORT.md` |
| 18B | Fixed Doctor URL refresh/persistence regressions and Trend tooltip hover target | `docs/SPRINT18B_REPORT.md` |
| 19 | **Universal search** â€” reusable `SearchBox` autocomplete (ILIKE live / includes snapshot, debounce, min-2, highlight, keyboard nav) + `/api/search` + `?q` page filtering on Diseases/Pharmacy/Diagnostics/Doctors | `docs/SPRINT19_REPORT.md` |
| 20 | **Executive Insights Panel** on Overview: deterministic alert bar, medication/lab/doctor Biggest Movers, and doctor Smart Comparison | `docs/SPRINT20_REPORT.md` |
| 21 | **Premium experience (UI only):** page transition (`PageTransition`), animated KPI count-up (`AnimatedNumber`, reused by all KPI cards), and Overview **Executive Summary** (3 deterministic observations) | `docs/SPRINT21_REPORT.md` |
| 23 | **Executive Command Palette** (`CommandPalette`, Ctrl+K / nav button) â€” reuses `/api/search` across all scopes and the existing `?q=` page filter to navigate; no new search/routing | `docs/SPRINT23_REPORT.md` |
| 24 | **Executive Experience** on Overview: scenario drawer, deterministic Explain controls, and Executive Feed using existing data only | `docs/SPRINT24_REPORT.md` |
| 25 | **Executive Compare Center** (`CompareCenter` drawer, âڑ– Compare button in `FilterBar`) â€” Doctor/Medication/Month vs comparisons built from the existing API routes; Quick Actions reuse existing routing/filters | `docs/SPRINT25_REPORT.md` |
| 26 | **Data Import Center** â€” Admin â†’ Data Import UI (`/admin/import`) over the existing importer; logic extracted to `lib/import-core.mjs` (single source of truth shared with `scripts/pg-import.mjs`); additive `/api/admin/import` (preview = parse only, import = streamed NDJSON progress) | `docs/SPRINT26_REPORT.md` |
| 27A | **Executive UX**: scenario drawer â†’ compact floating panel (X clears selection); Admin **Upload History** (localStorage, latest 10); **Settings** page (`/settings` â€” Account placeholder, Appearance, Dashboard toggles incl. functional Show-Executive-Feed, About with live DB status) | `docs/SPRINT27_REPORT.md` |
| 27B | **Design system documentation** â€” `HEALPATH_DESIGN_SYSTEM.md` (docs only, no code): brand philosophy, color/typography/spacing/radius/elevation/motion tokens (documenting the shipped `globals.css` system), component + dashboard + interaction + a11y rules, future-component placeholders. Inspired by an external automotive design doc (`DESIGN.md`), fully rebranded to HealPath | `docs/SPRINT27_REPORT.md` (Part B) |
| 28A | **Theme system (Dark Mode fix)** â€” `html[data-theme="dark"]` token remap in `globals.css`; `ThemeManager` applies the Settings Light/Dark/System preference (localStorage, live, OS-aware) with a no-flash `<head>` script; Overview insight cards + Executive Feed/Explain de-hardcoded to variables | `docs/SPRINT28A_REPORT.md` |
| 28B | **Premium Motion System** - shared motion tokens/keyframes for page transitions, Overview KPI stagger + hover, drawer/palette entrance, first-render chart entrance, skeleton loading placeholders, and restrained micro-interactions. No backend, SQL, API, routing, business logic, or layout redesign changes. | `docs/SPRINT28B_REPORT.md` |
| 29 | **Executive Performance Matrix** â€” new `/performance` page with shared matrix UI for Doctors, Specialties, and Medications; KPI cards, metric switcher, SearchBox, sorting, heatmap cells, hover tooltips, and DashboardContext/Executive Scenario row clicks. Reuses existing live queries only; no Laboratories tab, backend, SQL, API, or nav changes. | `docs/SPRINT29_REPORT.md` |
| 29A | **Performance Matrix Expansion** â€” extended `/performance` with Laboratories and Scans tabs using the same matrix component/interface, existing diagnostics SearchBox scope, heatmap/sorting/tooltips, and existing Executive Scenario panel. Added one read-only lab/scan entity KPI helper in `lib/queries.ts`; no schema, migration, API, backend, routing, or page redesign changes. | `docs/SPRINT29A_REPORT.md` |
| 29B | **Performance Matrix Aggregation Fix** â€” replaced per-cell `getKpis` calls with grouped entity-month aggregation via `getPerformanceEntityMetrics`, so every matrix cell represents Entity + Month and totals are computed per entity. No UI redesign, routing, API, schema, or unrelated page changes. | `docs/SPRINT29B_REPORT.md` |
| 30 | **Chronic Module Foundation** â€” added `/chronic` and a Chronic Care sidebar item with placeholders only for Executive Summary, KPI Cards, Trends, Issues, and Recommendations. Same Next.js app/Supabase/login/sidebar/theme/settings; no charts, tables, APIs, import, backend, or redesign changes. | `docs/SPRINT30_REPORT.md` |
| 31 | **Chronic Import Center** â€” added `/chronic/import` as a UI-only Excel validation preview for `Pre` and `Post` sheets, with required-column/week validation, Pre/Post summary metrics, disabled import button, and Sprint 32 backend message. No Supabase, SQL, API, upload, backend write, or redesign changes. | `docs/SPRINT31_REPORT.md` |

| 32 | **Chronic Import Backend** - connected `/chronic/import` to Supabase Postgres via `POST /api/chronic/import`; creates chronic Pre/Post/batch tables if missing, validates required fields, rejects duplicate Week + Patient ID + Medication Name records, imports Pre/Post rows transactionally, and returns imported rows/patients/weeks/duration. No dashboard, charts, KPIs, or redesign changes. | `docs/SPRINT32_REPORT.md` |
| 33 | **Chronic Executive Overview** - replaced the `/chronic` placeholder with a premium chronic executive overview backed by `getChronicOverview`: Month/Week/Consultant/Recommendation/Issue/Patient filters, animated KPI cards with WoW and mini trends, deterministic summary, alerts, Pre-to-Post movers, trend charts, and top issue/recommendation rankings. No Patient Explorer, matrix, drill-down, redesign, dashboard changes, or import behavior changes. | `docs/SPRINT33_REPORT.md` |
| 33A | **Chronic Import Navigation** - added a direct Import action in the Chronic Care header and browser-local Import History on `/chronic/import`, preserving the existing HealPath Import Center Upload -> Preview -> Import workflow. No database, API, dashboard, upload component rewrite, or redesign changes. | `docs/SPRINT33A_REPORT.md` |
| 33B | **Chronic Template Validation Fix** - updated `/chronic/import` validation to detect the uploaded workbook's real headers, including `INDIVIDUAL NUMBER` / `Individual NBR` patient IDs and numbered Issue columns, while ignoring optional Comment columns and showing detected column mappings in preview. No template, database, API, or redesign changes. | `docs/SPRINT33B_REPORT.md` |
| 33.5 | **Chronic Calendar Intelligence** - Month is the primary timeline; Week becomes internal (1–28). One reusable `lib/chronic-calendar.ts` maps week↔month (Dec 2025→Jun 2026, 4 weeks/month); import derives & stores `month_name`/`month_order` and returns Months Found / Weeks Found / Missing Weeks; `/chronic` Month filter (weeks removed) filters via the helper; preview shows Detected Months. Additive columns only; no DB redesign, new APIs, or page redesign. | `docs/SPRINT33_5_REPORT.md` |
| 33.6 | **Business Calendar Persistence** - the week→month mapping is now DATA in `healpath.chronic_calendar` (week, month_name, year, month_order, period), the single source of truth (seeded 1–28, idempotent). Import resolves each Week through it and stores month_name/year/period/month_order; dashboard filters use **Period** ("Jun 2026"). Adding weeks 29+ needs only new calendar rows — no code change (verified). `lib/chronic-calendar.ts` is now calendar-agnostic (seed + fallback); `GET /api/chronic/import` serves the calendar to the client preview. | `docs/CHRONIC_CALENDAR_PERSISTENCE_REPORT.md` |
| 34 | **Chronic Executive Dashboard v1** — `/chronic` upgraded to the complete executive dashboard: 9 KPI cards (incl. Pre/Post Patients + per-patient averages) with animated numbers, mini trends, delta vs previous period; deterministic Executive Summary; 4 fixed Executive Alerts; Biggest Movers (period vs period); 4 period trend charts; Top Issues/Recommendations with count + % + trend via BarRank. One-query period aggregation in `getChronicOverview`; existing components reused; no Patient Explorer/matrix/drill-down. | `docs/SPRINT34_REPORT.md` |
| 35 | **Chronic Data Normalization Layer** — `lib/chronic-normalizer.ts` (generic, reusable pipeline); every imported row passes `normalizeChronicRow()` before any DB write: 13 official Issue categories, explicit mapping rules, fuzzy ≥ 90%, unknowns kept; `Issue 1..N` fields in `row_data` + rebuilt `issue` column normalized; recommendation infrastructure (no rules yet); Rows/Normalized/Mapped/Unknown report after Validate + in the import response; `healpath.chronic_normalization_log` audit table (Original → Normalized → Reason per batch). No dashboard/KPI/SQL redesign. | `docs/SPRINT35_REPORT.md` |
| 33.5C | **Shared Chronic Parser** - created `lib/chronic-parser.ts` as the single chronic workbook parser. `/chronic/import` preview and `/api/chronic/import` import now share the same header normalization, aliases, Issue-column detection, validation, preview model, and parsed row output, removing the duplicate client/server validation pipeline. No database changes, API redesign, or UI redesign. | `docs/SPRINT33_5C_REPORT.md` |
| 35 | **Chronic Analytics** - added `/chronic/analytics` as a second chronic page with Issue, Recommendation, Consultant, and Medication analytics over existing chronic overview data. Reused existing cards, filters, BarRank, and inline trend chart styling; added derived in-memory analytics to `getChronicOverview`; no Patient Explorer, SQL, import, API route, or redesign changes. | `docs/SPRINT35_REPORT.md` |
| 36 | **Chronic Performance Optimization** - wired `/chronic` to `getChronicPageData`, an aggregate-only SQL path with parallel KPI/chart/filter/catalog/operational queries, no full chronic row fetch, no `row_data` shipped to Node, shared in-flight promise reuse across Suspense sections, and missing chronic indexes for patient_id/period/week/consultant/recommendation/issue. Old vs optimized output matched; data helper improved from 111,014 ms to 9,439 ms. No UI redesign, API change, import change, or business-logic change. | `docs/SPRINT36_REPORT.md` |
| 37 | **Executive KPI Redesign** - presentation-only polish for `/chronic` KPI cards: compact K/M values for PRE, POST, and Difference; green/red/gray improvement badges; stronger typography, spacing, and hierarchy; same responsive grid and HealPath card/motion shell. Operational KPI cards use the same executive treatment. No logic, query, API, import, or business-rule changes. | `docs/SPRINT37_REPORT.md` |
| 37.1 | **Executive KPI Cards Polish** - redesigned `/chronic` executive KPI cards only: taller 18px-radius cards, larger padding, more whitespace, equal PRE/POST columns, large bold compact numbers, muted Difference, Trend cue, rounded improvement badges, hover elevation, and responsive equal-height spacing. No calculations or business logic changed. | `docs/SPRINT37A_REPORT.md` |
| 38 | **Executive Charts** - added reusable chronic chart drill modal (`app/chronic/ExecutiveChartDrill.tsx`) for `/chronic` outcome charts and `/chronic/analytics` trend/BarRank/distribution charts. Modal is 80vw with large chart, zoom, pan, reset zoom, richer tooltip, legend, PNG/CSV download, close button, Esc, click-outside close, and responsive sizing. No chart calculation, query, API, import, database, or business-logic changes. | `docs/SPRINT38_REPORT.md` |
| 38 Patient 360 | **Patient 360** - added `/patient-360` with exact Patient ID search, patient summary cards, Overview/Timeline/Acute/Chronic/Medication History/Issues tabs, expandable acute and chronic rows, unified acute + chronic timeline, medication aggregation, fixed issue catalog, and sidebar placement between Chronic Care and Performance Matrix. One query-layer helper (`getPatient360`) uses multiple small parameterized queries to load related acute and chronic data with no N+1 UI fetching. No import, parser, auth, API route, schema, or existing dashboard changes. | `docs/SPRINT38_PATIENT360_REPORT.md` |
| 38.1 | **Executive Interactive Analytics** - upgraded the chronic chart drill modal with crosshair hover, selectable months/bars, richer hover values, smooth zoom/pan motion, a filtered drilldown analytics panel, and CSV export for selected drill context. `/chronic` outcome charts reuse existing `getChronicPageData` drilldown aggregates; no calculations, queries, APIs, imports, database, or business logic changed. | `docs/SPRINT38A_REPORT.md` |
| 39 | **Advanced Chronic Analytics** - added Executive Ranking Analytics to `/chronic/analytics`: Top Issues, Top Recommendations, Top Consultants, and Top Medications, each with Top 10 and Bottom 10 horizontal ranking bars plus Value/Label sort links that preserve all active filters. Uses existing filtered analytics data and chart components; consultant/medication pools widened for complete Bottom 10 coverage. No UI redesign, API, import, SQL/schema/database, or chart-calculation changes. | `docs/SPRINT39_REPORT.md` |
| 40 | **Patient Explorer** - added read-only `/chronic/patient` with Patient ID search, partial-match patient selection, summary cards, PRE to POST week timeline, Medication Changes, Issue History, Recommendation History, and Week History. Added `getChronicPatientExplorer` to fetch only the selected patient's PRE/POST rows and added a `/chronic` header Patient action. No editing, API, import, SQL/schema/database, auth, or dashboard KPI logic changes. | `docs/SPRINT40_REPORT.md` |
| 41 | **Executive Drilldown** - made every `/chronic` KPI card clickable with modal navigation over existing aggregate query output: Issues drill through weeks, consultants, patients, medications, and categories; Recommendations drill through categories, consultants, weekly trend, patients, and medications; operational KPIs use the same ranked drill surface. No new APIs or UI redesign. | `docs/SPRINT41_REPORT.md` |
| 42 | **Export Center** - added a filtered `/chronic` Export Center for Excel, CSV, PDF, and PNG covering dashboard KPIs, clinical outcome chart data, drilldown rankings, and fixed catalog tables. Uses existing `getChronicPageData` output client-side; no new APIs, import changes, schema changes, or business-logic changes. | `docs/SPRINT42_REPORT.md` |
| 43 | **Final Polish** - production-ready `/chronic` polish: matching route skeletons, retryable error boundary, empty catalog-table states, responsive/focus CSS, keyboard shortcuts, modal keyboard controls, memoized export derivations, and stale loading-code cleanup. Build passed with no functionality regression. | `docs/SPRINT43_REPORT.md` |
| 36R | **Chronic Dashboard Refactor** - refactored `/chronic` to match the uploaded Chronic Power BI business logic: PRE vs POST executive comparison, clinical outcome PRE/POST line charts, fixed full issue and recommendation catalog comparison tables, and operational KPIs. Removed generic analytics/rankings/movers from `/chronic`; no import, parser, API, auth, SQL/schema/database, Patient Explorer, or matrix changes. | `docs/SPRINT36R_REPORT.md` |
| 45 | **Executive Export Center Rewrite (ACUTE-only)** - replaced the unstable export implementation across the 7 acute dashboards (Overview, Disease & Diagnosis, Pharmacy, Doctor & Specialty, Labs & Scans, Trends, Performance Matrix). Chronic Care and Patient 360 are intentionally excluded. One shared `components/export/ExportToolbar` offers PDF / PNG / Full Report; `lib/export/*` captures dashboard DOM via html2canvas and builds A4-landscape PDFs (jsPDF, vector-text header/footer/pagination). Captures are gated by `waitForStableDashboard` (document loaded, fonts/images loaded, no skeletons, rendered charts/tables, animations finished, quiet DOM/layout). Full Report renders every dashboard through the same hidden same-origin iframe path for consistent dimensions and aborts on failure instead of skipping pages or emitting blank charts. PNG export now receives matching header/footer/margins. No DB/query/API/import/auth changes. Added deps remain html2canvas, jspdf. | `docs/SPRINT45_EXECUTIVE_EXPORT_CENTER_REPORT.md` |

---

## 9. Remaining sprints

1. **Optional data completeness:** load the 22 missing rows from a corrected extract.
2. **Cleanup (later):** remove dead `lib/supabase.ts` + `SQL` object; decide when to retire the snapshot (still required as the live fallback).

> Cross-filtering is active. URL state (`month`, `specialty`, `doctor`, `sel`, `selv`) is preserved across `Nav` navigation as of Sprint 18B.

> All snapshot-based data providers are now live. The snapshot remains only as the automatic fallback.

---

## 10. Known issues / caveats

- **22 fact rows unimported** â€” DB is 22 rows short of the workbook by design (source referential gaps). Documented in `docs/DATA_IMPORT_REPORT.md`.
- **`listMonths`/`listSpecialties`** are live via an **async-warmed cache** (Sprint 14): the sync accessors return the snapshot on the very first call(s) after a cold start, then the live values once the background query resolves (typically the next request). This is intentional (the sync contract forbids awaiting) and imperceptible â€” live months are identical to the snapshot, and live specialties differ only by the correct trimmed `Chest and Respiratory` (vs the snapshot's `\n`).
- **Low-severity parity gaps:** a few month-filtered Top-N visuals have tied rows in a different order than Power BI. Counts and labels match; documented in `docs/POWERBI_PARITY_REPORT.md`.
- **Doctor filter on the Doctors page is inert** by design (Sprint 15 scope): the dropdown/selection show there (global FilterBar + clickable doctor bars) but the page doesn't apply a doctor filter, so selecting a doctor there has no effect on that page's ranking/matrix. As of Sprint 18B, clicking a doctor still synchronizes DashboardContext, URL, and the Doctor dropdown via `?doctor=`, and that URL state persists when navigating to pages that do apply the doctor filter.
- **Selection vs dropdown priority (Sprint 17/18B):** an active chart selection overrides the same-dimension dropdown for `?sel/?selv` dimensions (e.g. selecting a specialty bar overrides `?specialty=`). Clearing the selection (re-click) restores the dropdown value. Doctor is the exception: doctor chart clicks use `?doctor=` and behave exactly like the Doctor dropdown.
- **Login is a real shared-password gate** (middleware + signed httpOnly cookie) as of 2026-07-19 — but only when `DASHBOARD_PASSWORD` is set; it is intentionally open otherwise. Single shared password, no per-user accounts/roles yet.
- **Dead code:** `lib/supabase.ts` and the `SQL` export in `lib/queries.ts` (exec_sql path abandoned).
- **Runtime DB connection:** direct `pg` from the Next server works locally via the pooler; for a serverless deployment, size the pooler connection limits and ensure `certs/prod-ca-2021.crt` ships with the app. `DATABASE_URL` must be the **Session Pooler** URI (direct host is IPv6-only and unreachable here).
- **Dev-server hygiene:** running `next build`/`next dev`/`next start` against the same `.next` concurrently corrupts the cache (caused an earlier "unstyled" scare). Stop other servers before building.
- **Environment:** Windows; use the Bash tool with the pooler for DB scripts. `dotenv`, `pg`, `@types/pg` are installed; `pg` is a runtime dependency.

---

*End of context. Keep this file updated at the end of each sprint.*
